import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { _ as _imports_24 } from "./wk7.tYF1-x9e.js";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/assets/dlwiniso.BxXucu9m.png";
const _imports_1 = "/assets/dlwiniso1.CQyxWlSu.png";
const _imports_2 = "/assets/general.BkOp2iK4.png";
const _imports_3 = "/assets/os.CX7bMEp5.png";
const _imports_4 = "/assets/system.C_AmkiPg.png";
const _imports_5 = "/assets/disks.BJniy5pO.png";
const _imports_6 = "/assets/cpu.DedcVQ_T.png";
const _imports_7 = "/assets/memory.CH7Gxf76.png";
const _imports_8 = "/assets/network.DPXM5W42.png";
const _imports_9 = "/assets/finish.Cf6DgPp1.png";
const _imports_10 = "/assets/start.vjgB4hml.png";
const _imports_11 = "/assets/gic1.XkdPXihx.png";
const _imports_12 = "/assets/gic2.s5VB7-xp.png";
const _imports_13 = "/assets/gic3.2zLznATL.png";
const _imports_14 = "/assets/gic4.CX3t8C19.png";
const _imports_15 = "/assets/gic5.pjwHWuhn.png";
const _imports_16 = "/assets/gic6.BnmyenL0.png";
const _imports_17 = "/assets/gic7.ZYHWzF5W.png";
const _imports_18 = "/assets/gic8.BwCbmRgC.png";
const _imports_19 = "/assets/gic9.62-9wTBo.png";
const _imports_20 = "/assets/net1.CUyp8ayS.png";
const _imports_21 = "/assets/net2.BP2m5XnQ.png";
const _imports_22 = "/assets/net3.BJFg7Me5.png";
const _imports_23 = "/assets/net4.CANZtcPE.png";
const _imports_25 = "/assets/virtio.CUVhk14O.png";
const _imports_26 = "/assets/lower2.SPGPKJ4N.png";
const _imports_27 = "/assets/lower1.DFw6c1iw.png";
const _imports_28 = "/assets/nodisk.CH47I8Tu.png";
const _imports_29 = "/assets/nodisk1.gZ09o2py.png";
const _imports_30 = "/assets/nodisk2.Do6XEKMW.png";
const _imports_31 = "/assets/nodisk3.1vBQc75S.png";
const _imports_32 = "/assets/nodisk4.CJjHGgtp.png";
const _imports_33 = "/assets/wk4.K5XvE6uQ.png";
const _imports_34 = "/assets/wk5.BEJ5BAWW.png";
const _imports_35 = "/assets/wk6.D9MKwk3w.png";
const _imports_36 = "/assets/wk1.BGnwsbrE.png";
const _imports_37 = "/assets/wk2.BasdNK2d.png";
const _imports_38 = "/assets/wk3.CWstwLHH.png";
const __pageData = JSON.parse('{"title":"Setting Up Windows Visual Machine","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"en/guide/pve/pve_windows.md","filePath":"en/guide/pve/pve_windows.md","lastUpdated":1769839579000}');
const _sfc_main = { name: "en/guide/pve/pve_windows.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="setting-up-windows-visual-machine" tabindex="-1">Setting Up Windows Visual Machine <a class="header-anchor" href="#setting-up-windows-visual-machine" aria-label="Permalink to &quot;Setting Up Windows Visual Machine&quot;">​</a></h1><h2 id="choose-kvm-virtualization-or-qemu-s-tcg-emulation-at-startup" tabindex="-1">Choose KVM virtualization or QEMU&#39;s TCG emulation at startup <a class="header-anchor" href="#choose-kvm-virtualization-or-qemu-s-tcg-emulation-at-startup" aria-label="Permalink to &quot;Choose KVM virtualization or QEMU&#39;s TCG emulation at startup&quot;">​</a></h2><p>The host itself, if it supports nested virtualization, can use <code>host</code> or <code>kvm64</code> or <code>qemu64</code> type CPUs</p><p>In terms of performance, <code>host</code> &gt; <code>kvm64</code> &gt; <code>qemu64</code></p><p>The <code>qemu</code> type of virtualization has a lot of performance loss</p><p>If you don&#39;t support nested virtualization, you can only use <code>qemu64</code> type CPU, and you need to change <code>KVM hardware virtualization</code> in <code>Options</code> before you start the VM</p><p>Set it to <code>no</code> to uncheck <code>Enabled[enabled]</code>.</p><p>If you&#39;re really not sure if your machine supports nested virtualization, then check System and Hardware Configuration Requirements --&gt; Detecting the Environment Detecting to ensure it&#39;s available (with attempts to enable the feature)</p><p>The main logic for this is in the</p><p><a href="https://raw.githubusercontent.com/oneclickvirt/pve/main/scripts/check_kernal.sh" target="_blank" rel="noreferrer">https://raw.githubusercontent.com/oneclickvirt/pve/main/scripts/check_kernal.sh</a></p><p>It is recommended to use PVE&#39;s System and Hardware Requirements --&gt; Environment Detection in this tutorial for one-click detection, or the following detection logic:</p><p><code>egrep -c &#39;(vmx|svm)&#39; /proc/cpuinfo</code> if it is 1</p><p><code>cat /sys/module/kvm_intel/parameters/nested</code> if it exists and is 1</p><p><code>lsmod | grep -q kvm</code> if it is loaded.</p><p>If all of these are supported, choose the <code>host</code> type to be 100% problem-free, in other cases there may be unknown errors try it yourself.</p><h2 id="using-iso-image-with-virtio-for-vm-setup" tabindex="-1">Using ISO Image with VirtIO for VM Setup <a class="header-anchor" href="#using-iso-image-with-virtio-for-vm-setup" aria-label="Permalink to &quot;Using ISO Image with VirtIO for VM Setup&quot;">​</a></h2><h3 id="_1-download-the-image" tabindex="-1">1. Download the Image <a class="header-anchor" href="#_1-download-the-image" aria-label="Permalink to &quot;1. Download the Image&quot;">​</a></h3><p>Before installation, you need to download the image file via <code>local(pve) --&gt; ISO images --&gt; Download from URL</code></p><p>For the download link (<code>URL:</code>), you can use files from: <a href="https://github.com/ILLKX/Windows-VirtIO" target="_blank" rel="noreferrer">https://github.com/ILLKX/Windows-VirtIO</a></p><p>Enter <code>win.iso</code> as the <code>File Name:</code></p><p><img${ssrRenderAttr("src", _imports_0)} alt="download" loading="lazy"></p><p>Click <code>Download</code>. After downloading is complete, you can see the file size of <code>win.iso</code> on the current page, confirming it&#39;s in ISO format.</p><p><img${ssrRenderAttr("src", _imports_1)} alt="downloaded" loading="lazy"></p><h3 id="_2-setting-up-the-template" tabindex="-1">2. Setting Up the Template <a class="header-anchor" href="#_2-setting-up-the-template" aria-label="Permalink to &quot;2. Setting Up the Template&quot;">​</a></h3><p>Click <code>Create VM</code> in the top right corner of the page.</p><p>In the <code>General</code> window, select <code>mypool</code> for <code>Resource Pool:</code>, enter <code>win</code> for <code>Name</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_2)} alt="general" loading="lazy"></p><p>In the <code>OS</code> window, select <code>win.iso</code> for <code>ISO image</code>. For <code>Guest OS</code>, select <code>Microsoft Windows</code> as the <code>Type</code> and choose the appropriate <code>Version</code> for your ISO. For example, if you downloaded Windows 2022, select <code>11/2022</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_3)} alt="OS" loading="lazy"></p><p>In the <code>System</code> window, select <code>VirtIO-GPU</code> for <code>Graphic card</code>, <code>q35</code> for <code>Machine</code>, <code>VirtIO SCSI</code> for <code>SCSI Controller</code>, and <code>Default (SeaBIOS)</code> for <code>BIOS</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_4)} alt="system" loading="lazy"></p><p>In the <code>Disk</code> window, select <code>Write Back</code> for <code>Cache</code>, enter your desired disk size in <code>Disk size (GiB)</code> (generally not less than <code>20</code>), and choose the storage location in <code>Storage</code>. In this example, only the system disk <code>local</code> is available, so <code>local</code> is selected. Then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_5)} alt="disk" loading="lazy"></p><p>In the <code>CPU</code> window, enter the number of cores needed in <code>Cores</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_6)} alt="cpu" loading="lazy"></p><p>In the <code>Memory</code> window, enter the desired memory size in <code>Memory (MiB)</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_7)} alt="memory" loading="lazy"></p><p>In the <code>Network</code> window, select <code>vmbr1</code> for <code>Bridge</code>, <code>VirtIO (paravirtualized)</code> for <code>Model</code>, uncheck <code>Firewall</code>, then click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_8)} alt="Network" loading="lazy"></p><p>In the <code>Confirm</code> window, click <code>Finish</code>.</p><p><img${ssrRenderAttr("src", _imports_9)} alt="finish" loading="lazy"></p><h3 id="_3-graphical-installation-configuration" tabindex="-1">3. Graphical Installation Configuration <a class="header-anchor" href="#_3-graphical-installation-configuration" aria-label="Permalink to &quot;3. Graphical Installation Configuration&quot;">​</a></h3><p>Click on the template you&#39;ve set up on the left, click <code>Start</code> in the upper right corner to start the virtual machine, then click <code>Console</code> to enter the <code>VNC</code> interface and wait for the operating system to boot.</p><p><img${ssrRenderAttr("src", _imports_10)} alt="console" loading="lazy"></p><p>In the VNC, click <code>Next</code>, then <code>Install now</code>, then <code>I don&#39;t have a product key</code>, check <code>Accept license</code>, click <code>Next</code>, and then select <code>Custom installation</code>.</p><p><img${ssrRenderAttr("src", _imports_11)} alt="win0" loading="lazy"><img${ssrRenderAttr("src", _imports_12)} alt="win1" loading="lazy"><img${ssrRenderAttr("src", _imports_13)} alt="win2" loading="lazy"><img${ssrRenderAttr("src", _imports_14)} alt="win3" loading="lazy"><img${ssrRenderAttr("src", _imports_15)} alt="win4" loading="lazy"></p><p>If the image comes with VirtIO drivers, you should be able to see the available system storage disk and its size. Select it and click <code>Next</code>.</p><p><img${ssrRenderAttr("src", _imports_16)} alt="win5" loading="lazy"><img${ssrRenderAttr("src", _imports_17)} alt="win6" loading="lazy"></p><p>Wait for the system to install. It may restart automatically several times and could take more than 10 minutes.</p><p><img${ssrRenderAttr("src", _imports_18)} alt="win7" loading="lazy"></p><p>After installation is complete, you&#39;ll be asked to set a password during first login.</p><p><img${ssrRenderAttr("src", _imports_19)} alt="win8" loading="lazy"></p><p>Once setup is complete, there&#39;s a pop-up box on the left side of the NOVNC page. Click the first button, then follow the login prompt by pressing <code>Ctrl</code>+<code>Alt</code>+last button to enter the login page.</p><h3 id="_4-network-initialization" tabindex="-1">4. Network Initialization <a class="header-anchor" href="#_4-network-initialization" aria-label="Permalink to &quot;4. Network Initialization&quot;">​</a></h3><p>Since the PVE is set up with a static network through this project, you need to manually modify the bound IP address after logging in, rather than using DHCP.</p><p>Right-click in the bottom right corner of the desktop and select <code>Open Network and Internet settings</code>. In the settings page that appears, click <code>Change adapter options</code>. <img${ssrRenderAttr("src", _imports_20)} alt="win9" loading="lazy"></p><p>In the <code>Network Connections</code>, select <code>Ethernet</code> and click <code>Change settings of this connection</code>. <img${ssrRenderAttr("src", _imports_21)} alt="win10" loading="lazy"></p><p>In the popup window, select <code>Internet Protocol Version 4 (TCP/IPv4)</code> and click <code>Properties</code>. <img${ssrRenderAttr("src", _imports_22)} alt="win11" loading="lazy"></p><p>In the new dialog box, select <code>Use the following IP address</code> and enter: IP address(I): <code>172.16.1.xxx</code> (replace xxx with your desired IP, I used 100 for convenience as my vmid is 100) Subnet mask(U): <code>255.255.255.0</code> Default gateway(D): <code>172.16.1.1</code></p><p>Then select <code>Use the following DNS server addresses</code> and enter:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>8.8.8.8</span></span>
<span class="line"><span>144.144.144.144</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Click OK in the bottom right corner, and make sure <code>Validate settings upon exit</code> is NOT checked.</p><p><img${ssrRenderAttr("src", _imports_23)} alt="win12" loading="lazy"></p><p>Click <code>OK</code> and <code>Yes</code> for any other dialog boxes, and your virtual machine will now have network connectivity.</p><h3 id="_5-unmounting-the-cd-drive" tabindex="-1">5. Unmounting the CD Drive <a class="header-anchor" href="#_5-unmounting-the-cd-drive" aria-label="Permalink to &quot;5. Unmounting the CD Drive&quot;">​</a></h3><p>Manually click on the corresponding <code>CD</code> in the <code>Hardware</code> on the web side of ProxmoxVE and select <code>Remove</code>, then restart the VM and you can use it directly.</p><p><img${ssrRenderAttr("src", _imports_24)} alt="wk" loading="lazy"></p><h2 id="using-iso-images-without-virtio-for-vm-installation" tabindex="-1">Using ISO Images without VirtIO for VM Installation <a class="header-anchor" href="#using-iso-images-without-virtio-for-vm-installation" aria-label="Permalink to &quot;Using ISO Images without VirtIO for VM Installation&quot;">​</a></h2><h3 id="_1-download-images" tabindex="-1">1. Download Images <a class="header-anchor" href="#_1-download-images" aria-label="Permalink to &quot;1. Download Images&quot;">​</a></h3><p>For Windows images without VirtIO, you can use:</p><p><a href="https://github.com/ILLKX/Windows" target="_blank" rel="noreferrer">https://github.com/ILLKX/Windows</a></p><p>For VirtIO ISO images, you can use:</p><p><a href="https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/archive-virtio/" target="_blank" rel="noreferrer">https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/archive-virtio/</a></p><p>Alternatively, you can download just the MSI package, package it into an ISO format locally, and then upload it to ProxmoxVE.</p><p>This way, the package is smaller. The official package contains support for all Windows images and is about 500MB, while a self-packaged one is around 5MB.</p><h3 id="_2-virtio-import" tabindex="-1">2. VirtIO Import <a class="header-anchor" href="#_2-virtio-import" aria-label="Permalink to &quot;2. VirtIO Import&quot;">​</a></h3><p>Note that in ProxmoxVE version 8.x and later, you can select VirtIO boot loading by default in the OS section, as shown in the image:</p><p><img${ssrRenderAttr("src", _imports_25)} alt="virtio" loading="lazy"></p><p>In lower versions of ProxmoxVE, such as 7.x and 6.x, you cannot select VirtIO boot loading by default in the OS section:</p><p><img${ssrRenderAttr("src", _imports_26)} alt="lower" loading="lazy"></p><p>You need to manually add <code>CD/DVD Drive</code> by clicking <code>Add</code> in the <code>Hardware</code> section and selecting the downloaded <code>VirtIO</code> <code>ISO</code> file:</p><p><img${ssrRenderAttr("src", _imports_27)} alt="lower" loading="lazy"></p><p>Then proceed as usual until you reach:</p><p><img${ssrRenderAttr("src", _imports_28)} alt="nodisk" loading="lazy"></p><p>At this point, you can&#39;t find the hard disk. You need to select <code>Load Driver</code>:</p><p><img${ssrRenderAttr("src", _imports_29)} alt="nodisk" loading="lazy"></p><p>Then select the <code>VirtIO</code> as your driver disk, click open, and import the corresponding system driver from the <code>amd64</code> directory:</p><p><img${ssrRenderAttr("src", _imports_30)} alt="nodisk" loading="lazy"></p><p>I&#39;m using a <code>Windows Server 2019</code> image, so I imported the <code>2k19</code> driver. After importing, select the imported driver and click <code>Next</code>:</p><p><img${ssrRenderAttr("src", _imports_31)} alt="nodisk" loading="lazy"></p><p><img${ssrRenderAttr("src", _imports_32)} alt="nodisk" loading="lazy"></p><p>Now you should be able to see the hard disk. Select it and continue with the installation steps as in the previous tutorial until the system installation is complete and you reach the desktop.</p><h3 id="_3-installing-drivers-via-virtio-win-guest-tools" tabindex="-1">3. Installing Drivers via virtio-win-guest-tools <a class="header-anchor" href="#_3-installing-drivers-via-virtio-win-guest-tools" aria-label="Permalink to &quot;3. Installing Drivers via virtio-win-guest-tools&quot;">​</a></h3><p>Once successfully installed and at the desktop, you still need to manually install the drivers.</p><p>Open the <code>VirtIO</code> driver&#39;s <code>CD</code> drive directory. At the bottom, there&#39;s a <code>virtio-win-guest-tools</code> program that will install the required VirtIO drivers, including the VirtIO network card driver:</p><p><img${ssrRenderAttr("src", _imports_33)} alt="wk" loading="lazy"></p><p><img${ssrRenderAttr("src", _imports_34)} alt="wk" loading="lazy"></p><p><img${ssrRenderAttr("src", _imports_35)} alt="wk" loading="lazy"></p><p>After clicking install, the network card driver will be installed. For subsequent network settings, refer to the network initialization tutorial mentioned earlier.</p><h3 id="_4-installing-network-card-driver-alternative" tabindex="-1">4. Installing Network Card Driver (Alternative) <a class="header-anchor" href="#_4-installing-network-card-driver-alternative" aria-label="Permalink to &quot;4. Installing Network Card Driver (Alternative)&quot;">​</a></h3><p>After successful installation and reaching the desktop, you may still need to manually install the network card driver to connect to the network:</p><ol><li>Open <strong>Control Panel</strong> → <strong>Device Manager</strong></li><li>Find the <strong>Ethernet Controller</strong>, right-click and select <strong>Update Driver</strong></li><li>Select <strong>Browse my computer for driver software (R)</strong></li><li>Browse to the <code>VirtIO</code> driver&#39;s <code>CD</code> drive directory</li><li>Open the <strong>NetKVM</strong> folder</li><li>Select the driver folder corresponding to your current system version (like the above operation, I would choose the 2k19 folder with amd64 drivers)</li><li>Confirm the installation, and after completion, the <code>VirtIO</code> network card driver will be successfully loaded</li></ol><p><img${ssrRenderAttr("src", _imports_36)} alt="wk" loading="lazy"></p><p><img${ssrRenderAttr("src", _imports_37)} alt="wk" loading="lazy"></p><p><img${ssrRenderAttr("src", _imports_38)} alt="wk" loading="lazy"></p><p>For subsequent network settings, refer to the network initialization tutorial mentioned earlier.</p><h3 id="_5-unmounting-the-cd-drive-1" tabindex="-1">5. Unmounting the CD Drive <a class="header-anchor" href="#_5-unmounting-the-cd-drive-1" aria-label="Permalink to &quot;5. Unmounting the CD Drive&quot;">​</a></h3><p>In the ProxmoxVE web interface, manually click on the corresponding <code>CD</code> in the <code>Hardware</code> section, select <code>Remove</code>, and then restart the virtual machine. This way, you can use it directly:</p><p><img${ssrRenderAttr("src", _imports_24)} alt="wk" loading="lazy"></p><h2 id="the-old-windows-image-only-includes-internet-explorer" tabindex="-1">The old Windows image only includes Internet Explorer. <a class="header-anchor" href="#the-old-windows-image-only-includes-internet-explorer" aria-label="Permalink to &quot;The old Windows image only includes Internet Explorer.&quot;">​</a></h2><p>Manually enter the following URL in IE: <code>https://www.microsoft.com/zh-cn/edge/download</code> to download the file.</p><p>Since IE does not automatically whitelist certificates, multiple pop-ups will appear. Click “Add Domain,” save the changes, and only then will the download button appear on the page.</p><p>Additionally, older systems lack the WebView2 component by default. A prompt will appear indicating that this component must be downloaded and installed separately to enable modern browser functionality.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/guide/pve/pve_windows.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const pve_windows = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  pve_windows as default
};
