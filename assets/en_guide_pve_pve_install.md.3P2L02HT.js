import{_ as t,c as i,o as s,ag as a}from"./chunks/framework.CSeR4K32.js";const u=JSON.parse('{"title":"PVE Body Installation","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"en/guide/pve/pve_install.md","filePath":"en/guide/pve/pve_install.md","lastUpdated":1765198116000}'),o={name:"en/guide/pve/pve_install.md"};function n(l,e,r,h,d,c){return s(),i("div",null,e[0]||(e[0]=[a('<h1 id="pve-body-installation" tabindex="-1">PVE Body Installation <a class="header-anchor" href="#pve-body-installation" aria-label="Permalink to &quot;PVE Body Installation&quot;">​</a></h1><p>If you don&#39;t know how to choose an option during installation, just press enter.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>For low-configuration hosts, it is recommended to check the contents of the <code>Custom</code> partition after all the content has been installed to perform memory tuning and reduce the memory footprint.</p></div><h2 id="installation-on-non-physical-machines" tabindex="-1">Installation on non-physical machines <a class="header-anchor" href="#installation-on-non-physical-machines" aria-label="Permalink to &quot;Installation on non-physical machines&quot;">​</a></h2><p>Proxmox installed by this method can subsequently use all scripts of this project.</p><h3 id="one-click-pve-installation" tabindex="-1">One-click PVE installation <a class="header-anchor" href="#one-click-pve-installation" aria-label="Permalink to &quot;One-click PVE installation&quot;">​</a></h3><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Suggest debian12+, the actual test part of the independent server debian11 system will appear a reboot network will be lost, debian12 does not have such a problem!</p></div><ul><li>The installation is the latest PVE from the apt source at the moment.</li><li>For example, debian10 is pve6.4, debian11 is pve7.x, debian12 is pve8.x, debian13 is pve9.x</li><li>Changes to the <code>/etc/hosts</code> file (to fix the wrong hostname setting for merchants and to add the required content for PVE)</li><li><code>/etc/cloud/cloud.cfg</code> file modification (to avoid overwriting modified hostname etc.)</li><li><code>/etc/network/interfaces</code> file modification (fix auto, dhcp type to static, add vmbr0 gateway)</li><li>Detect whether it is China IP, if it is China IP use Tsinghua mirror source, otherwise use the official source, and at the same time deal with the source of apt and the corresponding nameserver, to avoid disconnections</li><li>Create vmbr0 (independent IP gateway), the host allows addr and gateway for intranet IP or extranet IP, has been automatically recognized</li><li>vmbr0 creation support to open pure IPV4, pure IPV6, dual-stack virtual machine, automatic identification of IPV4 address and IPV6 address, automatic identification of the corresponding IP interval</li><li>Installation of the necessary toolkit for PVE to open a virtual machine</li><li>x86_64 replace enterprise subscriptions in apt sources with community sources, arm sources built using third-party patches for fixes</li><li>Print query Linux system kernel and PVE kernel installed or not</li><li>Setting up DNS detection <code>8.8.8.8.8</code> for boot add DNS systemd service</li><li>Download PVE and printout of login information after adding APT source link for PVE</li></ul><p>All modified files have been set to read-only mode to avoid overwriting after reboot.</p><p>If you want to modify the file, please use` <code>chattr -i file path</code> to cancel the read-only lock, and run <code>chattr +i file path</code> to lock the read-only lock when you finish modifying the file.</p><p>You will be prompted to reboot your system once during the execution process, <strong>After rebooting, be sure to wait at least 20 seconds to make sure the system does not reboot automatically again</strong>.</p><p>Because the original environment may be missing <code>ifupdown</code> or <code>ifupdown2</code> environment, there is a self-installation daemon loaded for the installation, after the installation of the system will automatically reboot the system again, wait for 20 seconds without reboot to ensure that the installation has been run.</p><p>If the host itself exists SLAAC assigned IPV6 address, will be able to choose whether to use the largest IPV6 subnet range, the default carriage return does not use the largest IPV6 subnet range only use the local IPV6, if you subsequently need to attach a separate IPV6 address to the virtual machine/container, the option must be selected <code>y</code>.</p><p>Command:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/oneclickvirt/pve/main/scripts/install_pve.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install_pve.sh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +x</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install_pve.sh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install_pve.sh</span></span></code></pre></div><p>or</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install_pve.sh</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>After successful installation, the web page may not be safe to open, click on Advanced or More Options and insist on accessing it!</p></div><p>The login information is your SSH account and password.</p><h3 id="pre-configure-the-environment" tabindex="-1">Pre-configure the environment <a class="header-anchor" href="#pre-configure-the-environment" aria-label="Permalink to &quot;Pre-configure the environment&quot;">​</a></h3><ul><li>Creating a resource pool mypool</li><li>Remove the subscription popup</li><li>Attempt to enable hardware passthrough</li><li>Detect and auto-install AppArmor modules.</li><li>Before rebooting the system, it is recommended to hook up <a href="https://github.com/naiba/nezha" target="_blank" rel="noreferrer">nezha probe</a> to facilitate the use of the command line in the background without SSH, to avoid the possibility that SSH may lead to the loss of the root password after the reboot due to the merchant&#39;s strange presets.</li><li>Before executing <code>reboot</code>, you need to wait for the background task to finish executing, some host system apt command execution is very slow, you have to wait for a while to finish executing, of course, most of the machines are not so bad!</li></ul><p>Command:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wget</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -qO-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-check-certificate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/oneclickvirt/pve/main/scripts/build_backend.sh)</span></span></code></pre></div><h3 id="automatically-configure-the-host-s-gateway" tabindex="-1">Automatically configure the host&#39;s gateway <a class="header-anchor" href="#automatically-configure-the-host-s-gateway" aria-label="Permalink to &quot;Automatically configure the host&#39;s gateway&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Before using this command, please make sure that you have restarted the server and that PVE can use the WEB terminal normally before executing this command. Do not execute this command immediately after restarting the machine, wait for at least 1 minute after the WEB terminal is successfully started before executing this command. If the WEB side doesn&#39;t start, run <code>systemctl status pveproxy</code> to see if it starts, if it&#39;s stuck, run <code>systemctl start pveproxy</code> to start the WEB side.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>This step is most likely to cause SSH disconnections, the reason is to modify the network without waiting for the PVE kernel to start, which will result in setting conflicts, so wait at least a few minutes until the kernel is started, that is, the WEB side is started successfully before execution.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>If the host needs to attach an IPV6 tunnel (add an IPV6 subnet to a host that does not have an IPV6 address) before executing this command, please check the <a href="https://www.spiritlhl.net/en/guide/incus/incus_custom.html#attach-free-ipv6-address-segments-to-host-machines" target="_blank" rel="noreferrer">IPV6 free subnet attachment</a> section to attach to the corresponding configuration file, but please <strong>ignore</strong> &#39;initial environment modifications&#39; and attach directly, and then execute the following one-click gateway configuration commands after verifying that you have an IPV6 address.</p></div><ul><li>If vmbr0 is not created, it is automatically created with the same logic as the main installation</li><li>Create vmbr1 (NAT gateway) to support IPV4 servers that open NAT for IPV6 networks with NAT.</li><li>Create vmbr2 (standalone IPV6 gateway), use ndppd to solve the problem of MAC verification of IPV6 addresses by the host, support the opening of servers with standalone IPV6 networks.</li><li>If you want to see the complete settings, you can execute <code>cat /etc/network/interfaces</code> to see, if you need to modify the gateway you need to modify the file, the web site can not be modified!</li><li>Load iptables and set back to source and allow NAT port forwarding.</li></ul><p>In short, <code>vmbr0</code> is responsible for v4 standalone IPs, and <code>vmbr1</code> is responsible for complex v4/v6 NATs, <code>vmbr2</code> is responsible for v6 standalone IPs.</p><p>Open independent IPV4 virtual machine using vmbr0, gateway with the host, IPV4/CIDR using the same network segment address and the same subnet mask, using the host&#39;s unbound IPV4 address for IPV4/CIDR, of course, if the subsequent use of this script does not need to pay attention to this point of the nuances of the thing</p><p>Use vmbr1 for IPV4 VM with NAT, <code>172.16.1.1</code> for gateway, <code>172.16.1.x/24</code> for IPV4/CIDR, where x can&#39;t be 1, but of course you don&#39;t need to pay attention to this minutia if you use this script later.</p><p>Command:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wget</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -qO-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-check-certificate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/oneclickvirt/pve/main/scripts/build_nat_network.sh)</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>This step may require you to reboot your system after a few minutes of successful execution, see the final execution of the script at the end of the tip. However, a reboot will ensure that some of the hidden settings are loaded successfully, so be sure to reboot the server once if you are in a position to do so.</p></div><p>To this point the main body is installed.</p><h2 id="installation-on-a-physical-machine" tabindex="-1">Installation on a physical machine <a class="header-anchor" href="#installation-on-a-physical-machine" aria-label="Permalink to &quot;Installation on a physical machine&quot;">​</a></h2><p>Proxmox installed by this method <strong>NOT</strong> to be used subsequently with all scripts of this project.</p><p>This method has not been tested and adapted on a large scale, only I installed PVE8.4 on the local machine, the router itself is not <code>automatically obtain an IP address</code> but <code>fixed IP address connection</code> way, if there is a problem corresponding to the warehouse open issues.</p><h3 id="u-disk-burning-official-iso" tabindex="-1">U disk burning official ISO <a class="header-anchor" href="#u-disk-burning-official-iso" aria-label="Permalink to &quot;U disk burning official ISO&quot;">​</a></h3><p>Official ISO download address:</p><p><a href="https://www.proxmox.com/en/downloads/proxmox-virtual-environment/iso" target="_blank" rel="noreferrer">https://www.proxmox.com/en/downloads/proxmox-virtual-environment/iso</a></p><p>You need to download it locally in advance.</p><p>You also need a USB flash drive with a good read/write rate for making a boot disk, as the boot disk needs to be formatted using rufus for boot disk making, you need to make sure that the USB flash drive is empty before making the disk</p><p>rufus download address (download to your local, not USB flash drive):</p><p><a href="https://rufus.ie/zh/" target="_blank" rel="noreferrer">https://rufus.ie/zh/</a></p><p>or</p><p><a href="https://github.com/pbatard/rufus" target="_blank" rel="noreferrer">https://github.com/pbatard/rufus</a></p><p>Burning needs to use DD method to burn, the original data of the USB flash disk will be completely erased after burning.</p><h3 id="pve-installation" tabindex="-1">PVE installation <a class="header-anchor" href="#pve-installation" aria-label="Permalink to &quot;PVE installation&quot;">​</a></h3><p>Find out how to get into the BIOS on the host itself, and modify two things after entering the BIOS</p><ol><li><p>Safe boot needs to be turned off</p></li><li><p>the order of the boot needs to be moved to the first USB order</p></li></ol><p>Then save the settings, then insert the USB flash drive, reboot the system, and choose to use the graphical interface for installation.</p><p>FQDN need to fill in a URL, you can fill in something like pve.spiritlhl.net, preferably a sub-domain of the domain name you own, not the actual domain name if you write it as something like pve.localsite.com will also work, the subsequent may not be used!</p><p>After the installation will automatically shut down and restart, reboot after the black screen can be unplugged USB, to avoid restarting the installation from the USB flash drive, of course, if you forget to reboot to the installation page, shut down the machine after unplugging the USB flash drive and then start up is also OK!</p><h3 id="wireless-network-configuration" tabindex="-1">Wireless Network Configuration <a class="header-anchor" href="#wireless-network-configuration" aria-label="Permalink to &quot;Wireless Network Configuration&quot;">​</a></h3><p>Download the required zip file and shell scripts</p><p><a href="https://cdn.spiritlhl.net/https://raw.githubusercontent.com/oneclickvirt/pve/refs/heads/main/extra_scripts/wireless/wireless.zip" target="_blank" rel="noreferrer">https://cdn.spiritlhl.net/https://raw.githubusercontent.com/oneclickvirt/pve/refs/heads/main/extra_scripts/wireless/wireless.zip</a></p><p><a href="https://cdn.spiritlhl.net/https://raw.githubusercontent.com/oneclickvirt/pve/refs/heads/main/extra_scripts/wireless/wireless.sh" target="_blank" rel="noreferrer">https://cdn.spiritlhl.net/https://raw.githubusercontent.com/oneclickvirt/pve/refs/heads/main/extra_scripts/wireless/wireless.sh</a></p><p>After downloading, unzip the zip file, drag the <code>wireless</code> folder into the root directory of a new USB flash drive, and the <code>wireless.sh</code> file has to be put into the root directory as well.</p><p>After the flash drive is plugged into the physical machine, you need to mount the flash drive, here sdx1 is the actual path of the flash drive queried by the first command, you need to modify it yourself</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fdisk</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/sdx1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /mnt</span></span></code></pre></div><p>The <code>wireless.zip</code> on the USB flash drive should be unzipped and opened to see the deb file.</p><p>At this point, run the one-click configuration</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /mnt/wireless.sh</span></span></code></pre></div><p>After the configuration is complete, the system will automatically reboot and the public network will be available after the reboot.</p><p>During the execution of the configuration script, you will be prompted to enter the name and password of the WIFI. Since there is no Chinese input method in the pure CI environment, the name of the WIFI must be composed of English numbers only, and the password must be the same.</p><h3 id="cautions" tabindex="-1">Cautions <a class="header-anchor" href="#cautions" aria-label="Permalink to &quot;Cautions&quot;">​</a></h3><p>Physical machine after the installation of unlimited modules can not use NAT to connect directly to the virtual machine to access the network, so the subsequent tutorials of this project does not support this method of access to the bridge, the subsequent scripts of this project does not support this method of installation of Proxmox.</p><p>Currently feasible wireless module used on the PVE success stories, are required to WIFI router access to the network is <code>automatically obtain an IP address (DHCP)</code> (the subsequent use of openwrt or ikuai or directly nat after the network available within the virtual machine), or can be modified in the router static routing table, if the WIFI router to use a <code>fixed IP address Connection to the network</code>, for the time being did not find success stories.</p>',69)]))}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
